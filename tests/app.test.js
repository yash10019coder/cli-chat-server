const request = require('supertest');
const app = require('../app');
describe('users-api', () => {
    it('POST /login', () => {
        expect(true).toBe(true);
    });
    it('POST /signup', () => {
        expect(true).toBe(true);
    });

});